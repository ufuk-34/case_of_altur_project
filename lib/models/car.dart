class Car {
  final String id;
  String name;
  String plate;
  String description;

  Car({
    required this.id,
    required this.name,
    required this.plate,
    required this.description,
  });
}